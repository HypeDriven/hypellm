[package]
name = "hypellm-config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Native line-oriented configuration grammar and validated snapshots."

[lints]
workspace = true

[dependencies]
hypellm-core = { path = "../hypellm-core" }
hypellm-fleet = { path = "../hypellm-fleet" }
hypellm-crypto = { path = "../hypellm-crypto" }

# The shared protocol corpus and the fuzz mutation engine (specification 21).
# Dev-only, and the corpus declares no dependencies of its own.
[dev-dependencies]
hypellm-test-corpus = { path = "../hypellm-test-corpus" }
