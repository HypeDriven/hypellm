[package]
name = "hypellm-fleet"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
hypellm-core = { path = "../hypellm-core" }
hypellm-crypto = { path = "../hypellm-crypto" }
wire-json = { path = "../wire-json" }

# The fuzz mutation engine and the fleet conformance corpus (specification 21).
# Dev-only, and the corpus declares no dependencies of its own.
[dev-dependencies]
hypellm-test-corpus = { path = "../hypellm-test-corpus" }

[lints]
workspace = true
